#include <stdio.h>
#include<math.h>
#include <stdlib.h>
int main() {
  int n;
  scanf("%d",&n);
  int fst=0;
  int sec=1;
  for(int i=0;i<n;i++){
    printf("%d ",fst);
    int nxt=fst+sec;
    fst=sec;
    sec=nxt;
  }
  return 0;

}
